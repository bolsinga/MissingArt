name: xcodebuild

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-12

    steps:
    - uses: actions/checkout@v2
    - name: Switch to Xcode 13.3
      run: sudo xcode-select -s "/Applications/Xcode_13.3.app"
    - name: Build
      run: xcodebuild -verbose -configuration Debug
